<header>
  <% if user_signed_in? %>
    <%= welcome_link(current_user) %>
    <%= badges_link(current_user) %>
    <%= link_to('Signout', destroy_user_session_path, method: :delete) %>
  <% else %>
    <%= link_to('Signin', new_user_session_path) %>
    <%= link_to('Signup', new_user_registration_path) %>
  <% end %>
</header>
