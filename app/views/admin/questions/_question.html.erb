<tr>
  <td><%= link_to(question.statement, admin_question_path(question)) %></td>
  <td><%= link_to('edit', edit_admin_question_path(question)) %></td>
  <td><%= link_to('delete', admin_question_path(question), data: { confirm: 'Are you sure?' }, method: :delete) %></td>
</tr>
